import RadioComponent from '../../../components/basic/RadioComponent';
import { MockCheckupList } from '../../../utils/mockData';

const RadioContent = () => {
  return <RadioComponent data={MockCheckupList} />;
};
export default RadioContent;
